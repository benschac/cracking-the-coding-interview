const Node = require('../Node/index.js');

/**
 * The Tree Data Structure
 * 
 * @class Tree
 */
class Tree {
  /** @inheritdoc */
  constructor(root) {
    this.root = root;
  }

  append(node) {
    this.root[direction] = new Node(node)
  }
}